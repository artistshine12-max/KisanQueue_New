# ─── Application ───────────────────────────────────────────────────────────────
APP_ENV=development                        # development | staging | production
DEBUG=true                                 # MUST be false in production
APP_VERSION=1.0.0

# ─── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5432/kisanqueue
# Driver: psycopg[binary] (psycopg3 async) — already in requirements.txt
# For Supabase (app — session-mode pooler):
#   postgresql+psycopg://postgres.[ref]:[password]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres
# For Supabase (Alembic migrations — direct connection):
#   ALEMBIC_DATABASE_URL=postgresql+psycopg://postgres.[ref]:[password]@db.[ref].supabase.co:5432/postgres

# ─── Authentication ────────────────────────────────────────────────────────────
JWT_SECRET_KEY=REPLACE_WITH_32_PLUS_RANDOM_BYTES_HEX
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440      # 24 hours for MVP

# ─── QR Token ──────────────────────────────────────────────────────────────────
QR_HMAC_SECRET=REPLACE_WITH_DIFFERENT_32_PLUS_RANDOM_BYTES_HEX
# Must be different from JWT_SECRET_KEY

# ─── OTP (Mock) ────────────────────────────────────────────────────────────────
OTP_MOCK_ENABLED=true                      # true = static OTP "1234"; false = real SMS
OTP_MOCK_CODE=1234                         # Only used when OTP_MOCK_ENABLED=true
OTP_EXPIRY_SECONDS=300

# ─── CORS ──────────────────────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:5173,https://kisanqueue.vercel.app
# Comma-separated; no trailing slashes

# ─── Rate Limiting ─────────────────────────────────────────────────────────────
RATE_LIMIT_OTP_PER_PHONE_PER_HOUR=5
RATE_LIMIT_GLOBAL_PER_USER_PER_MINUTE=60

# ─── Notifications ─────────────────────────────────────────────────────────────
NOTIFICATION_ADAPTER=mock                  # mock | whatsapp | sms

# ─── WhatsApp (Production only — leave blank for MVP) ──────────────────────────
WHATSAPP_PROVIDER=                         # twilio | meta_cloud
WHATSAPP_ACCOUNT_SID=
WHATSAPP_AUTH_TOKEN=
WHATSAPP_FROM_NUMBER=
WHATSAPP_META_ACCESS_TOKEN=
WHATSAPP_META_PHONE_NUMBER_ID=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=

# ─── SMS (Production only — leave blank for MVP) ───────────────────────────────
SMS_PROVIDER=                              # exotel | msg91 | twilio_sms
SMS_API_KEY=
SMS_SENDER_ID=

# ─── Government Integration ────────────────────────────────────────────────────
GOV_ADAPTER=mock                           # mock | euparjan | ekharid | anaaj_kharid
EUPARJAN_API_BASE_URL=
EUPARJAN_API_KEY=
EKHARID_API_BASE_URL=
EKHARID_API_KEY=

# ─── Mandi Price Subsystem ───────────────────────────────────────────────────
PRICE_PROVIDER=seed                        # seed | agmarknet | mock
AGMARKNET_API_KEY=                         # Official data.gov.in API key
AGMARKNET_BASE_URL=https://api.data.gov.in/resource/9ef84268-d588-465a-a308-a864a43d0070
AGMARKNET_TIMEOUT_SECONDS=10.0
AGMARKNET_MAX_RETRIES=3
PRICE_CACHE_TTL_HOURS=6

# ─── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO                             # DEBUG | INFO | WARNING | ERROR
LOG_FORMAT=json                            # json | text

# ─── Seed ──────────────────────────────────────────────────────────────────────
# SEED_ADMIN_PASSWORD=Demo@1234
# Default dev/demo password for seeded officers is "Demo@1234".
# In production, set SEED_ADMIN_PASSWORD in environment manager before seeding.
